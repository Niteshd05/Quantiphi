# All optional — the app runs fully with zero configuration.
PORT=5175
# Optional: unlocks ExchangeRate-API's keyed tier (higher limits, more frequent updates).
# Without it, the free open-access endpoint is used automatically.
EXCHANGERATE_API_KEY=
# Cache TTL for live rates, in seconds.
RATE_CACHE_TTL=600
